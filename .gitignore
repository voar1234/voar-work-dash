# ═══════════════════════════════════════════════════════════
#  외부 호스팅(GitHub Pages)에는 절대 올리면 안 되는 파일
# ═══════════════════════════════════════════════════════════

# 비밀 — 절대 push 금지
.env
db_config.json

# DB 접근·서버 코드 (내부 전용)
extract_dashboard_data.py
server.py
start_server.bat
서버시작.bat
서버시작_바로가기.lnk
_갱신.bat
_갱신_숨김.vbs
_갱신_log.txt
_媛깆떊_log.txt
_log.txt

# 디버깅·진단 스크립트 (외부 노출 불필요)
_*.py
_*.txt
_*.json
_*.sql
_*.bat

# 백업·일시 파일
*.bak.json
todos_*.bak.json
todos.broken_*.json
todos.before_*.json
todos.snap_*.json
events.snap_*.json
reviews_backup/
*.xlsx
*.pptx

# 캐시·자동 생성
__pycache__/
*.pyc
manuals/

# 육아 인스타 스튜디오 — index.html만 공개. 원본 데이터·설치 SQL은 내부 전용.
baby/data/
baby/supabase_setup.sql
# 단, 게시물 첨부용 배포 파일은 공개 (아래 *.xlsx 차단 규칙보다 뒤에 다시 선언)

# 다른 프로젝트 산출물 (별도 호스팅)
리뷰이벤트_CS.html
# 키워드_카테고리_대시보드.html  ← v6 (2026-08-06): GitHub Pages 배포로 공개
keyword_volumes.json
양성준_대시보드.html
last_upload.json
event_notes.json
events.json
mcp_mysql_client.py
_supabase_*.sql

# 사용자 안내·메모 (외부 공개 불필요)
README_담당자용.txt
팀원_안내.txt

# GitHub Pages는 index.html을 사용. 메인 대시보드 복사본을 index.html로 push.

# 인스타 배포용 첨부파일은 예외로 공개
!baby/posts/**/files/
!baby/posts/**/files/*.xlsx

# git 동시 실행 방지 잠금 (로컬 전용)
.gitlock/

# 노티 캐릭터 원본 — 공개 저장소에 올리지 않는다
_noti_master/
